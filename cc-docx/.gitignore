.DS_Store
__pycache__/
.scratch/
# workspace/ contains OneDrive-backed task data with real contact emails — kept local-only by
# choice (PII hook blocks commits). Compare snapshots with plain `diff`.
# Active projects live in workspace/ongoing/<yyMMdd>-<project>/; completed
# projects move intact to workspace/archived/<yyMMdd>/<yyMMdd>-<project>/.
workspace/
